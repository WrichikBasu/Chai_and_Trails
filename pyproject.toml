[project]
name = "chai-and-trails"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "django[argon2]>=6.1.1",
    "pillow>=12.3.0",
    "psycopg[binary,pool]>=3.3.5",
    "python-dotenv>=1.2.3",
]
